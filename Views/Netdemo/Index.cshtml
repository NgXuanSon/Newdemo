@model IEnumerable<Netmo.Models.Netdemo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NetdemoName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NetdemoName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NetdemoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NetdemoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NetdemoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
